import Section from "@/components/common/section";
import React from "react";

const HeroSection = () => {
  return (
    <Section id="hero">
      <div>HeroSection</div>
    </Section>
  );
};

export default HeroSection;
